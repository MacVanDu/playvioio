function showFeedback(type) { if (type === 'like') { notify('Thank you! We are glad you like it! ❤️', 'success'); } else { notify('Got it. We will try to improve the game! � ️', 'info'); } } function notify(message, type) { const toast = document.getElementById('custom-toast'); toast.innerText = message; toast.className = (type === 'success') ? 'toast-success show' : 'toast-info show'; setTimeout(() => { toast.classList.remove('show'); }, 3000); } (function() { const gameChat = document.getElementById('game-chat'); const gameId = gameChat.dataset.gameId; const todayKey = 'commented_' + gameId + '_' + new Date().toISOString().slice(0,10); const inputWrapper = document.getElementById('chat-input-container'); const sendBtn = document.getElementById('chat-send'); const usernameEl = document.getElementById('chat-username'); const messageEl = document.getElementById('chat-text'); if (sessionStorage.getItem(todayKey)) { showSuccessNotice(); } function showSuccessNotice() { inputWrapper.innerHTML = `<div class="chat-notice">✔ MISSION COMPLETE!<br>COMMENT AWAITING APPROVAL</div>`; } sendBtn.onclick = function () { const username = usernameEl.value.trim() || 'GUEST_HERO'; const message = messageEl.value.trim(); if (!message) return; if (sessionStorage.getItem(todayKey)) return; sendBtn.disabled = true; sendBtn.innerText = "..."; fetch(`/api/games/${gameId}/chat`, { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-CSRF-TOKEN': '{{ csrf_token() }}' }, body: JSON.stringify({ username, message }) }) .then(response => { if(response.ok) { sessionStorage.setItem(todayKey, '1'); showSuccessNotice(); if (typeof notify === 'function') { notify('DATA RECORDED SUCCESSFULLY!', 'success'); } } else { sendBtn.disabled = false; sendBtn.innerText = "GO!"; } }) .catch(() => { sendBtn.disabled = false; sendBtn.innerText = "GO!"; }); }; })(); document.addEventListener('DOMContentLoaded', function() { const desc = document.getElementById('gameDesc'); const btn = document.getElementById('gameDescBtn'); if (btn && desc) { btn.addEventListener('click', () => { desc.classList.toggle('expanded'); if (desc.classList.contains('expanded')) { btn.textContent = 'SHOW LESS'; btn.style.background = '#EA0404'; btn.style.color = '#fff'; } else { btn.textContent = 'VIEW MORE'; btn.style.background = '#f1c40f'; btn.style.color = '#000'; window.scrollTo({ top: desc.parentElement.offsetTop - 100, behavior: 'smooth' }); } }); } }); function open_fullscreen() { let game = document.getElementById("game-area"); const isIPhone = /iPhone|iPod/.test(navigator.userAgent) && !window.MSStream; if (isIPhone) { if (!document.getElementById("ios-fs-style")) { let style = document.createElement("style"); style.id = "ios-fs-style"; style.innerHTML = ` .ios-fullscreen { position: fixed !important; top: 0 !important; left: 0 !important; width: 100vw !important; height: 100vh !important; z-index: 999999 !important; border: none !important; } .ios-close-btn { position: fixed; top: 10px; right: 10px; z-index: 1000000; background: rgba(0,0,0,0.5); color: white; border: none; padding: 8px 15px; border-radius: 5px; font-family: sans-serif; } `; document.head.appendChild(style); } game.classList.add("ios-fullscreen"); let closeBtn = document.createElement("button"); closeBtn.innerText = "✖️"; closeBtn.className = "ios-close-btn"; closeBtn.onclick = function() { game.classList.remove("ios-fullscreen"); closeBtn.remove(); }; document.body.appendChild(closeBtn); } else { if (game.requestFullscreen) { game.requestFullscreen(); } else if (game.webkitRequestFullscreen) { game.webkitRequestFullscreen(); } else if (game.mozRequestFullScreen) { game.mozRequestFullScreen(); } else if (game.msRequestFullscreen) { game.msRequestFullscreen(); } } };